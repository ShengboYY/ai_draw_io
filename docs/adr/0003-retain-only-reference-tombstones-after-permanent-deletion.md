---
status: accepted
---

# 永久删除后只保留引用墓碑

永久删除资料时，系统删除文件名、原文件、解析派生物、证据内容、预览和向量，不为历史图表保留可恢复资料内容。为避免破坏图表结构，只留下由不透明标识、版本序号、删除时间和“来源不可用”状态组成的引用墓碑；已经生成在图表中的内容不会随来源删除而被自动改写。

## 结果

- 历史图表能够说明某条引用已失效，但不能继续展示来源名称、摘录、页码或预览。
- 影响分析依赖不透明标识和引用关系，而不能依赖已删除的资料元数据。
- 用户若要求清除已生成在图表中的信息，需要另外编辑或删除图表。
