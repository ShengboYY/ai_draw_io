# Domain Context

> 本文件记录本次多模态资料库需求中已经统一的领域语言，不包含具体实现方案。需求事实来源为本次产品讨论会话。

> **规范更新（2026-07-28）**：本文件仍可作为资料 owner/scope、lifecycle/retention、citation persistence 与 target resolution 的术语参考；RAG/多模态架构、turn admission、claim/fence、source demand、requiredness、fallback、snapshot timing 和 transport cancellation 以 [AI Draw.io 多模态 RAG 完整方案](docs/multimodal-rag-design.md) 为准。以下历史语义不得作为新实现合同：Personal Library 不属于 Project/Chartbook AUTO；不恢复逐消息来源 checkbox 或 `SourceMode` 选择器；snapshot 不在 Router/模型前冻结；断流只 detach，不取消产品 turn；所有 V2 turn 必须先完成 sticky assignment 与 atomic claim。

> 文中“自动引用”“指定引用”“仅使用所选资料”“来源策略”以及“Intent Router 先于 Source Probe”的定义是旧术语/旧顺序，保留仅为历史语境；新实现使用 typed source demand、Resolver、Pre-Planner 与 source-aware plan。
## Glossary

### 图表

一张独立画布及其对话、来源引用和固定资料版本。图表可以独立存在，也可以且最多属于一个图表册。

### 图表证据问答

发生在当前图表对话中的非画布变更问答。用户可以围绕图表整体、节点或连线及其关联资料继续追问；它不是独立的通用 RAG 聊天产品。

### 图表册

用户围绕一个主题组织多张图表的容器。图表册内的图表可以使用同一组共享资料。“共享”仅指在册内图表之间复用，不表示多人协作。

### 资料

用户上传或主动保存的逻辑文档或图片。资料归用户所有，可以有多个版本，并可通过关联出现在个人资料库、图表册或图表中。

### 资料库

用户长期保存和复用资料的独立产品入口。它支持上传、搜索、版本管理、引用查看、回收站和向图表册添加资料。产品名称使用“资料库”，不使用“知识库”。

### 个人资料库

资料库中属于当前用户、可跨图表和图表册使用的长期资料范围。它与“本图资料”和“图表册共享资料”相区别。

### 本图资料

随当前图表保留、只服务于该图表及其当前和后续对话的资料作用域。它默认不进入个人资料库。

### 图表册共享资料

可被同一图表册内多张图检索的资料作用域。资料仍归用户所有，移出图表册只解除关联。

### 匿名工作区

未登录访客使用的独立工作范围，只有持有对应服务端匿名凭证的访客才能访问。它可以拥有图表和临时资料，但不能拥有本图资料、个人资料库或图表册；长期保留资料需要先登录。

### 匿名凭证

由服务端签发、用于证明访客控制某个匿名工作区的秘密凭证。匿名工作区标识本身不是访问证明。

### 临时资料

逻辑上跟随一个图表会话、在无有效活动 24 小时后过期且不会自动转为长期资料；它与随图表长期保留的本图资料不同。登录用户的临时资料到期后进入 30 天回收站，匿名临时资料到期后直接永久删除且不可恢复。

### 资料关联

资料与个人资料库、图表册或图表之间的关系。关联用于决定检索边界，不代表复制文件；解除关联不等于删除资料。

### 资料版本

同一逻辑资料下，由用户明确执行“上传新版本”而创建的不可变内容快照。普通上传不会仅因文件名相同而自动归入已有资料；同一所有权范围内完全相同的内容复用已有版本。已有图表固定使用具体版本，新图默认使用最新可用版本。

### 处理修订

同一资料版本经过一次完整处理后形成的不可变证据解释。资料内容未变而处理方法变化时产生新修订；历史引用仍指向当时使用的修订，直到被主动重新验证。

### 等待资料

会话内上传所触发的对话请求已被接受，但在其依赖的资料全部达到可用状态前暂停检索和生成的状态。用户在消息流中看到处理进度，并可停止当前回复。

### 部分就绪

资料处理已经结束，但只有部分页面或模态可用的终态。依赖请求不会自动使用可用部分，必须由用户在获知缺失范围后明确选择继续。

### 会话内上传

在图表对话中发起、与当前请求直接关联的资料上传。依赖该资料的当前回复可以进入等待资料状态，并在资料就绪后自动继续。

### 资料管理入口上传

从资料库或图表册资料入口发起的长期资料上传。资料处理与任何单个对话请求解耦，未就绪前不能被依赖它的请求使用。

### 证据单元

能够支持回答或图表结论的最小可追溯文本、图片、图表或表格片段。证据单元关联资料版本、页码、区域、摘录和模态。

### Retrieval Chunk

为了提高召回效果而建立的检索表示。它可以为一个或多个证据单元增加标题上下文，也可以是章节或文档桥接单元；检索命中后必须映射回真实证据单元，合成或桥接文本不能直接形成来源引用。

### 来源引用

图表元素或回答结论与证据单元之间的关系。只有实际参与结果生成的证据才能形成来源引用。

### 引用单位

能够独立绑定来源引用的最小结果对象。首版中，图表以语义节点或连线为引用单位，回答以独立结论或段落为引用单位。

### 来源冲突

两个或多个来源对同一对象、同一时间和同一适用范围给出无法同时成立的结论。因时间或适用范围不同而产生的差异不称为来源冲突。

### 引用墓碑

资料永久删除后，为维持历史图表的引用结构而保留的不可恢复记录。它只说明某个资料版本曾被引用且现已不可用，不保留文件名、摘录、预览或其他资料内容。

### 自动引用（历史术语，已废止）

用户未明确选择资料时，系统在当前允许的本图、图表册和个人资料库范围中寻找相关证据，并在实际使用后标注来源。

### 指定引用（历史术语，已废止）

用户明确选择一个或多个资料或版本作为当前任务优先依据的方式。

### 仅使用所选资料（历史术语，已废止）

严格来源约束。启用后，系统只能使用用户明确选择的资料，不得以个人资料库自动检索、联网搜索或 AI 常识补齐事实。

### 来源策略（历史术语，已废止）

当前请求能够使用哪些资料、联网来源和 AI 常识的有效规则。安全与生命周期约束优先，其次依次采用当前请求、当前对话、当前图表、所属图表册、用户和系统层级的设置。

### 联网引用

实际参与生成的网页来源记录，包括标题、URL、访问日期和支持具体结论的短摘录，但不包含完整网页快照。只被搜索但未使用的网页不构成联网引用。

### Source Probe（旧顺序定义，已由多模态 RAG 完整方案取代）

意图识别前的轻量资料探测。它只读取权限、选择、版本、处理状态、类型、页数和质量概况等可信元数据，不读取正文或图片内容。

### Intent Router（旧顺序定义，已由多模态 RAG 完整方案取代）

识别用户主要动作、复合步骤和画布变更边界的意图路由器。它先于内容检索运行，以建立可信的工具和画布权限。

### Diagram Target Resolver

在意图和权限确定后，将用户当前选择或文字指代解析为图表中的具体节点或连线。它可以得到唯一目标、多个候选或找不到目标；后两种结果需要目标澄清，不能直接检索或修改画布。

### Retrieval Router

在意图和权限确定后，选择不检索、文本检索、视觉检索或混合检索的路由器。

### Retrieval Query Plan

在已解析图表目标和已确定来源策略下，对当前问题形成的有限检索计划。它描述查询类型、最多三个信息 facet、模态、精确术语和预算，但不能添加资料版本或改变授权范围。

### 混合召回

在同一已授权资料集合中并行使用 Pinecone 语义向量和 MySQL 关键词/精确术语检索，再通过基于名次的 RRF 融合候选。两路命中都必须经过相同的 MySQL 二次鉴权、读取租约和原文回源。

### 复合请求

一个用户目标中包含多个有顺序关系的任务，例如“先总结文章，再画流程图”。一个回合最多执行一次主要画布变更。

### 读取租约

AI 任务开始读取资料时取得的短期有效凭证。资料到期或进入删除流程后不再颁发新租约，但已有有效租约可完成，以避免后台清理删除正在使用的数据。

### 回收站

符合保留条件的资料在永久删除前所处的可恢复状态，进入后立即停止参与新检索，并在 30 天后永久清理。匿名临时资料不进入回收站。

### 已验证

来源引用状态之一，表示当前图表语义与其固定证据一致。

### 需要复核

来源引用状态之一，表示资料版本或处理修订发生主动切换后，原证据关系尚未重新验证。用户直接修改语义不会进入此状态。

### 人工内容

来源状态之一，表示内容由用户直接创建，或由用户修改后已解除原有当前引用，因而当前没有外部证据。后续问答可以把它作为问题上下文，但不能把它本身当作事实证据。

### 无外部引用

来源状态之一，表示内容由 AI 常识补充且没有使用可追溯的文件或网页证据。它不同于用户直接创建的人工内容。

### 部分有依据

结果级的证据覆盖状态，表示同一回答或图表中既包含有外部证据支持的结论，也包含明确标记为无外部引用的结论。

### 来源不可用

来源引用状态之一，表示原资料已永久删除、权限失效或证据无法再读取。
