<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.zipp.ai.infrastructure.dao.IModelCredentialMapper">

    <resultMap id="modelCredentialMap" type="org.zipp.ai.infrastructure.dao.po.ModelCredentialPO">
        <id column="id" property="id"/>
        <result column="user_id" property="userId"/>
        <result column="provider" property="provider"/>
        <result column="base_url" property="baseUrl"/>
        <result column="model" property="model"/>
        <result column="completion_path" property="completionPath"/>
        <result column="display_name" property="displayName"/>
        <result column="encrypted_api_key" property="encryptedApiKey"/>
        <result column="encryption_provider" property="encryptionProvider"/>
        <result column="encryption_key_id" property="encryptionKeyId"/>
        <result column="encryption_nonce" property="encryptionNonce"/>
        <result column="key_last_four" property="keyLastFour"/>
        <result column="status" property="status"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
        <result column="disabled_at" property="disabledAt"/>
        <result column="deleted_at" property="deletedAt"/>
    </resultMap>

    <sql id="cols">
        id,
        user_id,
        provider,
        base_url,
        model,
        completion_path,
        display_name,
        encrypted_api_key,
        encryption_provider,
        encryption_key_id,
        encryption_nonce,
        key_last_four,
        status,
        created_at,
        updated_at,
        disabled_at,
        deleted_at
    </sql>

    <insert id="insert" parameterType="org.zipp.ai.infrastructure.dao.po.ModelCredentialPO">
        INSERT INTO model_credential (
            id, user_id, provider, base_url, model, completion_path, display_name,
            encrypted_api_key, encryption_provider, encryption_key_id, encryption_nonce, key_last_four,
            status, created_at, updated_at, disabled_at, deleted_at
        )
        VALUES (
            #{id}, #{userId}, #{provider}, #{baseUrl}, #{model}, #{completionPath}, #{displayName},
            #{encryptedApiKey}, #{encryptionProvider}, #{encryptionKeyId}, #{encryptionNonce}, #{keyLastFour},
            #{status}, #{createdAt}, #{updatedAt}, #{disabledAt}, #{deletedAt}
        )
    </insert>

    <select id="selectByUserId" resultMap="modelCredentialMap">
        SELECT <include refid="cols"/>
        FROM model_credential
        WHERE user_id = #{userId}
          AND deleted_at IS NULL
        ORDER BY updated_at DESC, created_at DESC
    </select>

    <select id="selectByUserIdAndId" resultMap="modelCredentialMap">
        SELECT <include refid="cols"/>
        FROM model_credential
        WHERE user_id = #{userId}
          AND id = #{id}
          AND deleted_at IS NULL
        LIMIT 1
    </select>

    <update id="disable">
        UPDATE model_credential
        SET status = 'DISABLED',
            disabled_at = #{disabledAt},
            updated_at = #{disabledAt}
        WHERE id = #{id} AND user_id = #{userId} AND deleted_at IS NULL
    </update>

    <update id="delete">
        UPDATE model_credential
        SET deleted_at = #{deletedAt},
            updated_at = #{deletedAt}
        WHERE id = #{id} AND user_id = #{userId} AND deleted_at IS NULL
    </update>

    <update id="deleteAllForUser">
        UPDATE model_credential
        SET user_id = #{anonymizedUserId},
            encrypted_api_key = '',
            encryption_nonce = '',
            key_last_four = '',
            status = 'DISABLED',
            disabled_at = COALESCE(disabled_at, #{deletedAt}),
            deleted_at = COALESCE(deleted_at, #{deletedAt}),
            updated_at = #{deletedAt}
        WHERE user_id = #{userId}
    </update>

</mapper>
