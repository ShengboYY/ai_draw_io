<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.zipp.ai.infrastructure.dao.IAdminAuditLogMapper">

    <resultMap id="auditLogMap" type="org.zipp.ai.infrastructure.dao.po.AdminAuditLogPO">
        <id column="id" property="id"/>
        <result column="actor_user_id" property="actorUserId"/>
        <result column="action" property="action"/>
        <result column="target_type" property="targetType"/>
        <result column="target_id" property="targetId"/>
        <result column="outcome" property="outcome"/>
        <result column="ip_address" property="ipAddress"/>
        <result column="user_agent" property="userAgent"/>
        <result column="created_at" property="createdAt"/>
    </resultMap>

    <sql id="cols">id, actor_user_id, action, target_type, target_id, outcome, ip_address, user_agent, created_at</sql>

    <insert id="insert" parameterType="org.zipp.ai.infrastructure.dao.po.AdminAuditLogPO">
        INSERT INTO admin_audit_log (
            id, actor_user_id, action, target_type, target_id, outcome, ip_address, user_agent, created_at
        )
        VALUES (
            #{id}, #{actorUserId}, #{action}, #{targetType}, #{targetId}, #{outcome},
            #{ipAddress}, #{userAgent}, #{createdAt}
        )
    </insert>

    <select id="listRecent" resultMap="auditLogMap">
        SELECT <include refid="cols"/> FROM admin_audit_log
        ORDER BY created_at DESC, id DESC
        LIMIT #{limit}
    </select>

</mapper>
