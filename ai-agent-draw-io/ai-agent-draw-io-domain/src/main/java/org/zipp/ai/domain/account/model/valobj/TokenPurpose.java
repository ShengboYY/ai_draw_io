package org.zipp.ai.domain.account.model.valobj;

/** Purpose of a one-time {@code account_token}. Kept as an enum so lookups are always purpose-scoped. */
public enum TokenPurpose {

    EMAIL_VERIFY,
    PASSWORD_RESET

}
