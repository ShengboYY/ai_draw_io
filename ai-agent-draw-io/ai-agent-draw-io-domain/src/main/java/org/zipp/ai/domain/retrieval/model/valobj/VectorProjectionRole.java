package org.zipp.ai.domain.retrieval.model.valobj;

/** Whether a revision projection is its publication lane or a generation-upgrade compatibility lane. */
public enum VectorProjectionRole {
    PRIMARY,
    COMPATIBILITY
}
