package org.zipp.ai.domain.retrieval.model.valobj;

/** Publication readiness of one revision projected into one index generation. */
public enum RevisionVectorProjectionState {
    BUILDING,
    READY
}
