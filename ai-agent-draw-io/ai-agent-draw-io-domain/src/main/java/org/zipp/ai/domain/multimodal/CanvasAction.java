package org.zipp.ai.domain.multimodal;

/** The canvas action already authorized by intent routing; source choice is deliberately separate. */
public enum CanvasAction {
    CREATE,
    EDIT,
    ANSWER,
    REVIEW,
    OPTIMIZE_LAYOUT
}
