// Keep the existing Candidate implementation as the write-model compatibility
// route while Trace Analysis exposes the canonical user-facing Findings URL.
export { default } from '../eval-candidates/page';
